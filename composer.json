{
    "name": "ygor/crm",
    "description": "CRM Empresarial - framework próprio em PHP",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.5",
        "laravel/pint": "^1.29",
        "phpstan/phpstan": "^2.2"
    },
    "scripts": {
        "test": "phpunit",
        "analyse": "phpstan analyse --memory-limit=1G",
        "analyse:baseline": "phpstan analyse --memory-limit=1G --generate-baseline",
        "format": "pint",
        "format:test": "pint --test"
    },
    "scripts-descriptions": {
        "analyse": "Analise estatica (PHPStan nivel 5). O --memory-limit e obrigatorio: com os 128M padrao a analise aborta no meio e reporta zero erro, parecendo aprovada.",
        "format": "Formata com Pint. Ainda NAO adotado: reescreveria 65 dos 97 arquivos, entao rode so quando decidir bancar esse commit."
    }
}
